<Project Sdk="Microsoft.NET.Sdk">
  <ItemGroup>
    <PackageReference Include="Dapper" Version="2.1.86" />
    <PackageReference Include="Microsoft.Extensions.Diagnostics.HealthChecks" Version="10.0.12" />
    <PackageReference Include="Npgsql" Version="10.0.3" />
  </ItemGroup>
  <ItemGroup>
    <!-- The read model's DDL is owned by db/search-schema.sql; the API applies it idempotently on startup. -->
    <EmbeddedResource Include="..\..\db\search-schema.sql" LogicalName="search-schema.sql" />
  </ItemGroup>

</Project>
